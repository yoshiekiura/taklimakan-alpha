
        {% if lectures is defined and lectures is not empty %}

            <div class="leftside-box">

                <h3>Course Content</h3>

                {% for lecture in lectures %}

                <div class="last-cource-body bb-1">
                  <div class="row">
                      {#
                      <div class="col-xl-3 col-lg-3 col-md-12 col-sm-12 col-12">
                          <div class="last-cource-title mobile-overflow">
                            { # <img src="/images/lectures/{{lecture.image}}" alt=""> # }
                            <img src="/images/folder.png" alt="">
                        </div>
                      </div>
                      #}
                      {# <div class="col-xl-6 col-lg-6 col-md-8 col-sm-8 col-8"> #}
                      <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                          <div class="last-cource-title">
                            <a href="/lectures/{{lecture.id}}/{{lecture.title|translit}}" class="mb-10">| #{{lecture.number}} | {{lecture.title}}</a>
                            {# FIXME <a href="#" class="news-source edu-cat">Cryptocurrencies</a> #}
                        </div>
                      </div>
                      {# NB! Do we need lecture pricing here ?
                      <div class="col-xl-3 col-lg-3 col-md-4 col-sm-4 col-4 text-right">
                          <h3 class="main-price fs-16">
                            { { lecture } }
                        </h3>
                      </div>
                      #}
                  </div>
                </div>

                {% endfor %}

            </div>

        {% endif %}
